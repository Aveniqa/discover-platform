# Next 16 can request legacy flat item RSC payloads during client navigation.
# The static export keeps the real per-item RSC payloads but strips the flat
# `/item/<slug>.txt` copies to stay under Cloudflare Pages' 20,000-file cap.
/item/:slug.txt /item/:slug/__next.item.$d$slug.__PAGE__.txt 200
